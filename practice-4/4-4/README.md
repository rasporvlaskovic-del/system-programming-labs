# Задание 4-4: Анализ памяти

Выводы описаны в `analysis.txt`. Утилита мониторинга через `/proc/[PID]/status` наглядно показывает, что параметр `VmRSS` (Resident Set Size - объем физической ОЗУ) существенно падает после вызова `free()`. Это подтверждает корректный возврат памяти операционной системе.