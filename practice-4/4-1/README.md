# Задание 4-1: Динамическая память

**Запуск:**
1. `gcc -Wall -Wextra -o task task.c`
2. `./task`

**Результат:** Память под 50 структур `Student` успешно выделяется с помощью `malloc()`, проверяется на NULL, заполняется циклом и корректно освобождается вызовом `free()`.