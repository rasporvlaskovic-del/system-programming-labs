# Задание 3-2: Именованные каналы (FIFO)

**Запуск:**
1. `gcc -Wall -Wextra -o task task.c`
2. `./task`

**Результат:** Родительский процесс генерирует 50 чисел и пишет в `myfifo`. Дочерний процесс считывает их, находит минимум/максимум и удаляет файл канала через `unlink()`.